# I1.10 — 286-page E2E Proof Run Findings (Zeggo VRV IV REYQ)

## ✅/⚠️ [QA phase-1-qa-report.md FOLLOW-UP, 2026-08-10] F1-F4 status

QA's Q1.1/Q1.2 re-audit (`Documentation/qa-reports/phase-1-qa-report.md`)
found 4 issues in the SA1.2 fixes below — status after this session's
follow-up work:

- **F3 (daemon running stale code)** — **FIXED**. `paddleocr-vl-service`
  restarted from current code; confirmed the residual NULLs are
  independent of daemon staleness (see F2 below).
- **F2 (`visual_description` — corrected numbers + new finding)** —
  **investigated, root-caused, honestly NOT fully fixed**. QA's corrected
  count for document_id=3 alone: **381/386 (98.7%)**, 5 genuine NULLs (not
  8 — the original 8 included 3 elements that actually belong to a
  different document, a query bug on my part). All 5 re-confirmed NULL
  against the fresh (F3) daemon, root-caused to a genuine PaddleOCR-VL
  layout-detection limitation on dense full-bleed diagrams (not a code
  bug) — see `paddleocr-vl-service/README.md` for the full investigation,
  including a fallback that was tested live and explicitly NOT shipped
  because it was unacceptably slow (7-22+ minutes per call, unbounded).
- **F1 (`content_structured.rows` empty for HTML tables)** — **FIXED**.
  `chunker.py` now parses real PaddleOCR-VL HTML table-reparse output (not
  just markdown). Re-verified: 157/157 table chunks now have non-empty
  `rows` (was 141/157). See commit `46505e7`.
- **F4 (official `docker compose --profile gpu` path never verified)** —
  **build+health+GPU-passthrough now verified working**; found and fixed 2
  real bugs (build-context/`.dockerignore`, missing native shared
  libraries) in the process. **New, unresolved finding**: real-inference
  latency inside the container was NOT shown to be viable (20+ minute
  stalls, root cause unconfirmed) — see `paddleocr-vl-service/README.md`
  F4 section for full detail. The bare-metal WSL process remains the
  verified-fast serving path for now.

## ✅ [SA1.2 RE-VERIFICATION, 2026-08-09] Both real bugs fixed, clean re-ingest of document_id=3

System Analyst's SA1.2 spot-check (`Documentation/system-design/02-ingestion-pipeline.md`
§6.2) found two real bugs in the original run below by querying real
Postgres data, not just reviewing code: (1) `visual_description.description`
NULL for 386/386 Stage 4 elements, and (2) 61/163 (37%) icons with
`parent_id = NULL` becoming orphan empty-text chunks. Both are now fixed
(see `app/ingestion/paddleocr_vl_cascade.py`, `app/ingestion/docling_parser.py`,
`paddleocr-vl-service/app/pipeline.py` for the code changes and inline
root-cause docstrings) and **re-verified against a clean re-ingest of the
same document** (existing `pages`/`elements`/`chunks` for document_id=3
deleted, Qdrant points for `document_id=3` cleared, full pipeline re-run
end-to-end — not a partial/retroactive patch, since Stage 2's page-hash
idempotency skip would otherwise have prevented the corrected
`parent_local_id`/`visual_description` from ever being (re-)written):

```
OrchestratorResult(document_id=3, pages_stored=286, elements_stored=2916,
                    chunks_stored=1417, chunks_embedded=1417,
                    cascade_task_count=402)
elapsed_seconds=4905.2  (81.75 minutes)
documents.status = "ready"
```

| Stage | Duration |
|---|---:|
| `native_probe` | 5.9s |
| `docling` | 552.4s (9.2 min) |
| `paddle_cascade` | 3848.2s (64.1 min) — 402 tasks this time (was 386): `cascade_task_count` rose because `THRESHOLD_TABLE` was also recalibrated 0.75→0.90 this session (SA1.1), which queues *more* `table_reparse` tasks (real table confidences cluster between 0.75-0.90, per the I1.6 benchmark distribution) — an expected, unrelated side effect, not a regression |
| `kg_candidate` | 0.4s |
| `chunking` | 9.2s |
| `embedding` | 304.9s (5.1 min) |

**Bug #1 (`visual_description.description` NULL) — fixed**:

```sql
select count(*) filter (where visual_description->>'description' is not null) as has_desc, count(*) as total
from elements where extraction_method='paddle_vlm';
-- has_desc=381, total=389  (was 0/386 before the fix)
```

The remaining 8/389 NULLs were spot-checked individually (element ids 2, 4,
6, 5184, 5186, 5188, 6051, 6130 — pages 1-3 and a couple of mid-document
pages) — these are plausible genuine "nothing to describe" results (e.g.
title-page logo/blank-region images), not a recurrence of the mapping bug;
not claimed as 100% out of honesty, but the fix demonstrably works for the
overwhelming majority (98%) where the old code produced 0%.

**Bug #2 (icon `parent_id` cross-page fallback) — fixed**:

```sql
select count(*) filter (where parent_id is null) as null_parent, count(*) as total_icons
from elements where element_type='icon';
-- null_parent=0, total_icons=163  (was 61/163 before the fix)
```

`chunks_stored` dropped from 1478 (original run) to 1417 — exactly 61
fewer, matching the eliminated orphan-icon-chunk count. Re-verified against
the **exact reproduction case SA1.2 cited** (page 8, 6 icons, no text
element of its own): all 6 icons (element ids 5002/5004-5008) now resolve
`parent_id=4994` (the "Pictograms" heading from page 7) instead of `NULL`,
and are merged into chunk id 1501 (`page_start=7, page_end=9`,
`content_text` starts with `"Pictograms"`, 16 total `element_ids`) — the
exact outcome SA1.2 predicted the fix should produce.

**Third item (folder relocation)**: `paddleocr-vl-service` moved from a
`vrf-chat/` top-level sibling to `vrf-chat/backend/paddleocr-vl-service/`
(`git mv` preserving history; `.venv` physically relocated + shebang paths
fixed; `docker-compose.yml` build context updated) — verified by restarting
the service from the new location (`GET /health` OK) before running this
re-ingest, so this run itself is also proof the relocated service works.

**Fourth item (`THRESHOLD_TABLE` recalibration, SA1.1)**: this run's
`cascade_task_count` increase (386→402) is itself incidental evidence the
new `0.90` default is live and taking effect (more tables now correctly
flagged for re-parse than the old `0.75` default would have caught).

## ✅ FINAL RESULT (original run, superseded by the re-verification above for bug-fix purposes, kept for the 100%-coverage/no-fatal-error DoD record): full 286-page end-to-end ingestion succeeded

```
OrchestratorResult(document_id=3, pages_stored=286, elements_stored=2916,
                    chunks_stored=1478, chunks_embedded=1478,
                    cascade_task_count=386)
elapsed_seconds=4033.4  (67.2 minutes)
documents.status = "ready"
```

Per-stage breakdown (`ingestion_jobs.stage_metrics`):

| Stage | Duration |
|---|---:|
| `native_probe` | 6.1s |
| `docling` | 418.6s (7.0 min) |
| `paddle_cascade` | 3125.1s (52.1 min) — 386 real PaddleOCR-VL inference calls, the dominant cost, as expected |
| `kg_candidate` | 0.3s |
| `chunking` | 0.7s |
| `embedding` | 300.4s (5.0 min) — 1478 chunks, real fastembed dense+sparse + real Qdrant upsert |

Real Qdrant `vrf_chunks` collection confirmed holding the embedded points
(1484 total, including a few from earlier smoke-test documents in the same
collection). Real Postgres holds all 286 `pages`, 2916 `elements`, 1478
`chunks` rows with full traceability (`element_ids` -> `elements.id` ->
`pages.page_number` -> `documents.id`) and real object storage crops for
every figure/icon element.

**This satisfies Fase 1's Definition of Done item**: "Minimal 1 dokumen
penuh... berhasil diingest end-to-end... tanpa error fatal, sebagai bukti
pipeline utuh" (`02-phase-1-ingestion.md`).

### What it took to get here (fixes applied this session, all committed)

1. **`PADDLE_OCR_VL_TIMEOUT_SECONDS` 60s → 180s → 420s** (evidence-based,
   see §2-3 below) — some real cascade tasks (complex diagrams/tables,
   under near-6GB VRAM pressure) genuinely take longer than 180s to
   generate; confirmed via 100% GPU utilization throughout (real
   computation, not a hang).
2. **`paddleocr-vl-service` async event-loop blocking fixed** — route
   handlers changed from `async def` to synchronous `def` (FastAPI
   dispatches these to a threadpool automatically), and
   `PaddleOCRVLPipeline`'s lock now covers the *entire* predict call, using
   an `RLock`. Live-verified: `GET /health` responded in 0.001-0.23s while
   a real inference request was actively running (previously hung 120+s
   before this fix) — see `paddleocr-vl-service/README.md`.
3. **Retry logic added** (`PADDLE_OCR_VL_MAX_RETRIES`, now defaulting to 1)
   for genuine transient failures, separate from the "just slow" cases
   §1 addresses.
4. **VRAM risk explicitly documented** (peak ~5.96-5.98GB of 6.14GB,
   investigated for further reduction — none found beyond what was already
   applied) in `paddleocr-vl-service/README.md`, per explicit instruction
   not to leave this unstated.
5. **Process durability**: the final successful run was executed as a
   true detached daemon process in WSL (`nohup ... & disown`, not tied to
   any interactive tool-call session) after two earlier attempts with the
   *same* fixes got killed by what appeared to be environment/session
   infrastructure churn unrelated to the ingestion code itself (both prior
   attempts had already reached `paddle_cascade: done` / `kg_candidate: done`
   before being killed, which is what gave confidence to retry rather than
   suspect a code bug).

### Original diagnostic history (§1-4 below), preserved as-is

The sections below are the original, real-time diagnostic record from
before the fixes above were applied — kept for the historical record of
what was found and how, not because the "partial"/"failed" conclusions
below are still current.

> Status (original): **partial** — Stage 1/2 (native_probe, Docling) fully
> proven at full 286-page scale, twice, consistently. Stage 4 (PaddleOCR-VL
> cascade via `paddleocr-vl-service`) makes real progress (many individual
> cascade tasks succeed) but the run does not complete end-to-end — a
> specific, understood, and documented remaining issue, not a mystery. See
> §3 for the concrete root cause and recommended fix (now applied, see
> update above).

## 1. What was proven, twice, against the real 286-page document

Run via `scripts/i1_10_run_e2e.py` (WSL-native `uv` venv, real Postgres
[WSL Docker, port 15432], real Qdrant [port 6333], real
`paddleocr-vl-service` [port 8100], real local object storage, real Docling
on GPU):

| Stage | Run 1 | Run 2 |
|---|---:|---:|
| `native_probe` | 6.9s | 5.9s |
| `docling` | 401.9s (6.7 min) | 430.0s (7.2 min) |
| `paddle_cascade` | failed after 768.5s (12.8 min) of real progress | failed after 1301.5s (21.7 min) of real progress |

**`native_probe` and `docling` are fully proven at full scale** — both runs
processed all 286 pages successfully, consistently, in the 6-7 minute
range. This is **substantially faster** than the naive linear extrapolation
from the I1.6 50-page benchmark (`239.2s/50 pages -> ~1367s/286 pages`) —
plausible explanation: the I1.6 sample was deliberately stratified toward
visually-complex pages (40% electrical_diagram/screenshot_ui), while the
real corpus average is more text-heavy and processes faster per page. This
itself is useful data for System Analyst's ongoing threshold/throughput
calibration (not a contradiction of I1.6, a refinement of it — a
non-stratified full-document number).

Earlier (separate, smaller) proof: a full 3-page extract completed the
**entire** pipeline successfully end-to-end (native_probe → docling →
cascade_trigger → paddle_cascade → canonical_store → kg_candidate →
chunking → embedding) against the same real infrastructure —
`OrchestratorResult(document_id=2, pages_stored=3, elements_stored=6,
chunks_stored=6, chunks_embedded=6, cascade_task_count=3)`. This confirms
the full architecture (all 10 I1.x pieces + the new `paddleocr-vl-service`
+ real Qdrant hybrid embedding) is correct and wired together properly —
the remaining gap is a **throughput/robustness** issue at full-corpus
cascade volume, not a correctness issue in the pipeline logic itself.

## 2. Two real bugs found and fixed during this run (both already committed)

1. **`PADDLE_OCR_VL_TIMEOUT_SECONDS` default (60s) too short.** Real
   single-image PaddleOCR-VL inference regularly takes 30-90s+. Raised
   default to 180s (`app/core/config.py`).
2. **No retry on transient timeout.** Added `PADDLE_OCR_VL_MAX_RETRIES`
   (default 2) to `RemoteAPIPaddleOCRVLClient._post` — verified via unit
   test that a request failing once then succeeding is transparently
   recovered. This measurably helped (Run 2 made more progress than Run 1:
   21.7 min vs 12.8 min before failing) but did not fully solve it — see §3.

## 3. Remaining issue — root cause understood, fix not yet implemented

Both runs eventually hit a `describe_figure`/similar request that timed out
on **all 3 attempts** (180s × 3 = up to 9 minutes for that one element)
before the orchestrator gave up and marked the stage `failed`. Two
contributing factors, both diagnosed with reasonable confidence from
observed behavior (not fully instrumented/confirmed on the service side —
flagged honestly as the limit of what was verified):

1. **Some individual images are genuinely slow to process**, consistently
   (not a one-off cold-start — the same request failed 3/3 times within one
   `_post` call). Plausible causes: a large/complex electrical schematic
   generating a long markdown description (VLM token generation time scales
   with output length), or cumulative slowdown after many sequential
   requests (thermal, memory fragmentation — not confirmed).
2. **`paddleocr-vl-service`'s FastAPI route handlers are `async def` but
   call fully synchronous, blocking pipeline code directly** (`app/main.py`
   `describe_figure`/`reparse_table`/`ocr_page`). This blocks Python's
   single-threaded asyncio event loop for the **entire duration** of one
   inference call — confirmed by observing that `GET /health` (which does
   no GPU work at all) also hung for 120+ seconds while a slow inference
   request was in flight. This means requests cannot be served concurrently
   even for trivial endpoints, and likely compounds any genuine per-image
   slowness into apparent "stuck" behavior from the caller's perspective.

**Recommended fix** (not implemented in this session — concrete, scoped
follow-up): change `paddleocr-vl-service/app/main.py`'s route handlers from
`async def` to plain `def` — FastAPI automatically dispatches synchronous
route handlers to a thread pool, which would let `/health` (and, more
importantly, the idle-unload watcher's ability to observe state) stay
responsive during a slow inference call, and is a well-understood, low-risk
change. This does not by itself fix "some images take a very long time" —
that likely needs either a longer timeout+fewer-but-longer retries, or
(more robust) an async job-submission pattern (`POST` returns immediately
with a job id, caller polls) instead of a single long-blocking HTTP call —
a larger design change, appropriately left to System Analyst/PM to weigh in
on given the effort involved, not decided unilaterally here.

## 4. Definition of Done — honest assessment

Per `02-phase-1-ingestion.md`'s Fase 1 DoD: **"Minimal 1 dokumen penuh
(disarankan salah satu yang lebih pendek, mis. Zeggo VRV IV REYQ, 286
halaman) berhasil diingest end-to-end... tanpa error fatal, sebagai bukti
pipeline utuh"** — this is **not yet fully met** for the full 286-page
document (Stage 4 does not complete). It **is** met for:
- The full pipeline logic/architecture, proven on a real 3-page extract of
  the same document with the same real infrastructure.
- Stages 1-2 independently, at full 286-page scale, reproducibly.

Recommended as the concrete next step for a continuation session: apply the
`async def` -> `def` fix above (small, well-understood), then re-run this
same script — given Run 2 already made 21.7 minutes of real progress before
failing, a working concurrent-request-capable service should very plausibly
complete the remaining volume in a similar or shorter timeframe.
